# El_GUI_Bandito

Кастомный аналог Stream Deck на базе Raspberry Pi 3B+ (Client) и PC (Server).

## 📋 Описание
Проект состоит из двух частей:
1.  **Client (RPi):** Python + PySide6 приложение с сенсорным интерфейсом. Выступает в роли пульта управления.
2.  **Server (PC):** FastAPI приложение на Windows. Принимает команды от RPi по WebSocket и управляет системой (звук, запуск программ, эмуляция нажатий).

### 📸 Интерфейс / UI Screenshots

<table>
  <tr>
    <td align="center"><b>Server Side (PC)</b></td>
    <td align="center"><b>Client Side (RPi)</b></td>
  </tr>
  <tr>
    <td>
      <img src="https://github.com/user-attachments/assets/c65f116d-4375-4ed0-b0ce-0ea1d86ca6cb" width="450" alt="Server UI" />
    </td>
    <td>
      <img src="https://github.com/user-attachments/assets/bbf5ecfe-0fe6-4fe2-ac39-840f4edbe6e1" width="450" alt="Client UI" />
    </td>
  </tr>
  <tr>
    <td align="center"><i>Интерфейс сервера для управления системой</i></td>
    <td align="center"><i>Сенсорный интерфейс пульта управления</i></td>
  </tr>
</table>

## 🏗 Архитектура
*   **Связь:** WebSocket (двусторонняя).
    *   Server: `FastAPI` (Python)
    *   Client: `QtWebSockets` (C++ binding)
*   **UI:** Qt Designer -> `.ui` файлы -> автоматическая компиляция в `.py`.
    *   Стилизация через внешние JSON файлы (Dark Theme, Network Status).
*   **Конфигурация:** 
    *   Сохранение настроек в JSON (раздельно для Server и Client).
    *   Шифрование чувствительных данных (паролей).
*   **Настройки:** 
    *   Встроенная валидация IP и портов.
    *   Инструмент диагностики сети (Ping) с парсингом результатов и отображением статуса.

## 📂 Структура проекта
*   `el_cliento/` - Исходный код клиента (запускается на Raspberry Pi).
*   `el_bandito/` - Исходный код сервера (запускается на Windows).
*   `src/manager_plugin.py` - Логика управления плагинами (UI: `plugin_manager.ui`).
*   `resources/ui_raw/` - Исходные файлы дизайна (.ui). Сюда сохранять из Qt Designer.
*   `resources/ui_done/` - Скомпилированные файлы (.py). **Не редактировать вручную!**
    *   `ui_bandito/` - UI файлы сервера.
    *   `ui_cliento/` - UI файлы клиента.
*   `src/utilts.py` - Общие утилиты (в т.ч. компилятор UI).
*   `configs/` - Файлы конфигурации JSON.

## 🚀 Установка и Запуск

### Требования
*   Python 3.10+
*   PySide6
*   FastAPI, Uvicorn (для сервера)

### Запуск (Dev Mode)
При запуске `el_bandito.py` или `el_cliento.py` система автоматически проверит и перекомпилирует изменившиеся `.ui` файлы.

**Server (PC):**
```bash
python el_bandito/el_bandito.py
```

**Client (RPi):**
```bash
python el_cliento/el_cliento.py
```
