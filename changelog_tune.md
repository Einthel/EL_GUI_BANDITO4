## [0.0.0.6] - 2026-03-08

### Added
- **Интеграция с Core:**
  - Конструктор `TuneBanditoPlugin(plugin_path, core=None)` принимает опциональный `core` от ядра.
  - Рассылка клиентам выполняется через `core.com.broadcast("TUNE_CONFIG_UPDATE", self.config)` вместо прямого `requests.post`.
  - При отсутствии `core` выводится краткое предупреждение, broadcast не выполняется.

### Changed
- **Логи (оптимизация):**
  - Префикс логов сокращён до `[Tn]`.
  - В `tune_bandito.py`: убраны избыточные сообщения (Save button pressed, Client pressed, Selecting device); оставлены только краткие сообщения об ошибках и состоянии.
  - В `tn_audio_manager.py`: вместо нескольких строк при обновлении устройств вывода выводится одна строка `[Tn] Output devices count: N`. Остальные логи (Requesting..., Default device detected, перебор устройств) убраны.
  - При переключении системного устройства вывода (`set_default_output_device`): убрана строка «Switching...», при успехе выводится одна строка `[TuneBandito] Default output device set to: {device_name}`.

## [0.0.0.5] - 2026-03-08

### Added
- **Realtime-синхронизация конфига:**
  - Broadcast `config_tune.json` при загрузке плагина и при любом сохранении конфигурации.
  - Клиент обрабатывает `TUNE_CONFIG_UPDATE`, сохраняет конфиг на диск и эмитит `config_updated`.
- **Клиент — аудиоустройства:**
  - Заполнение `audiD_01_lineE` и `audiD_02_lineE` из `config_tune.json` (ключ `output_devices`).
  - Подсветка выбранного устройства через dynamic property `selectedDevice` и стиль `QLineEdit[selectedDevice="true"]` в `style_tune_cliento.json`.
- **Кнопки выбора устройства (Cliento → Bandito):**
  - `audiD_01_toolB` и `audiD_02_toolB` отправляют `PLUGIN_BUTTON_PRESS` на сервер.
  - Сервер определяет устройство по индексу (0/1), устанавливает его системным по умолчанию, сохраняет в конфиг и рассылает обновление клиентам.

## [0.0.0.4] - 2026-03-08

### Added
- **Аудио-устройства (Bandito, сервер):**
  - Автоопределение активного системного аудиовыхода при запуске плагина и логирование полного списка устройств.
  - Сохранение списка выходных устройств и текущего активного (`selected_device`) в `config_tune.json`.
  - Заполнение комбобоксов `audiD_01_comboB` и `audiD_02_comboB` с гарантированной уникальностью выбранных устройств и приоритетом устройства по умолчанию.
  - Ручная привязка аудио-устройств через UI с сохранением в конфиг по нажатию `output_device_save_toolB`.
  - Переключение системного устройства вывода Windows на выбранное в конфиге (`selected_device`) через `libs.audio_manager.audioSwitch`.
  - Отправка обновлённого конфига клиентам через `POST /api/broadcast` в формате `command + data`.

## [0.0.0.3] - 2026-03-08

### Added
- **Стилизация (JSON CSS):**
  - Реализована полная поддержка внешних стилей через JSON-конфигурации для серверной (`style_tune_bandito.json`) и клиентской (`style_tune_cliento.json`) частей.
  - Внедрена поддержка `qproperty-icon` и `qproperty-iconSize` непосредственно в JSON-стилях для управления иконками кнопок.
- **Интеграция:**
  - В `TuneBanditoPlugin` добавлен механизм динамической загрузки и применения стилей при инициализации.
  - В `TuneClientoPlugin` автоматизирована подгрузка иконок (`mic_mute_toolB`, `sound_mute_toolB`, `other_mute_toolB`) через систему стилей, исключая жесткое кодирование путей в Python.

## [0.0.0.2] - 2026-03-02

### Added
- **Плагины:**
  - **Sound:**
    - Создана базовая структура плагина (папки, манифест).
    - Реализован UI клиента (`ui_tune_cliento.py`) с элементами управления громкостью, микрофоном и эквалайзером.
    - Добавлена логика загрузки и применения стилей из `style_tune_cliento.json`.
    - Подготовлены ресурсы (иконки, системные файлы).
    - Интеграция в систему переключения плагинов клиента.

## [0.0.0.1] - 2026-02-19
