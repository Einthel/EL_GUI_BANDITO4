# ToDo General

## 📝 Planned / In Progress
- [ ] **Стабильность:** Выяснить причину падений при переключении плагинов. Реализовать подробнейшее логгирование всех этапов инициализации и переключения слотов.
- [ ] **Refactoring Auto-Update:** Оптимизировать систему обновлений — сделать код более "изящным" и архитектурно чистым (убрать "костыли", рассмотреть атомарные обновления).
- [ ] **Безопасность:** Заменить простую обфускацию паролей (Base64+XOR) на надежное шифрование (например, Fernet/cryptography) в `src/manager_save_load.py`.
- [ ] **Оптимизация UI:** Перенести выполнение `subprocess.run` (Ping) в отдельный поток (`QThread`), чтобы интерфейс не "фризился" во время выполнения, несмотря на `processEvents`.
- [ ] **Локализация:** Улучшить парсинг вывода Ping. Сейчас он ищет слова "Average" или "Среднее". На других языках ОС (DE, FR, CN) это не сработает.

## ✅ Completed (Achievements)
### [2026-01-18/19]
- [x] **Архитектура:** Разделение проекта на `el_bandito` (Server) и `el_cliento` (Client).
- [x] **Настройки:** Реализованы окна настроек для обоих приложений с валидацией полей (IP, Port, Login).
- [x] **Сохранение:** Внедрен `ConfigManager`. Настройки (IP Source/Dest, Port, Login, Password) сохраняются в JSON и восстанавливаются при запуске.
- [x] **Сеть:** Добавлена возможность ручного выбора IP-адреса источника (`IP Source`).
- [x] **Диагностика:** Реализован инструмент Ping (4 пакета) с выводом результатов (успех/ошибка/среднее время) прямо в интерфейс. Поддержка кириллицы в консоли Windows (`cp866`).


# ToDo Sound

## Следующие шаги
- Индикация уровней (Peak Meter): Реализовать передачу пиковых уровней аудиосигнала (vumeter) от сервера к клиенту для визуализации активности звука.
- Управление микрофоном: Расширить логику SoundCore для полноценной поддержки нескольких входных устройств (сейчас фокус на выводе).
- Hotkeys: Интеграция с плагином shortcut для управления громкостью/Mute через глобальные горячие клавиши.
- Автоматизация: Добавить "пресеты" громкости для разных сценариев (например, "Стрим", "Тихий режим").